{
  "name": "lectores-parroquia",
  "version": "1.0.0",
  "private": true,
  "description": "Planificador de lectores y celebraciones de la parroquia",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "migrate:reader-preferences": "node scripts/migrate-reader-preferences.js --apply",
    "test": "node test/server.test.js",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.4.2",
    "mongodb": "^6.21.0"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
